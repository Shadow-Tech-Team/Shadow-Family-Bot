import got from "got";

export default class Meme {

    constructor (client, interaction) {
        this.client = client;
        this.interaction = interaction
    }

    async Make() {
        got
    }

}